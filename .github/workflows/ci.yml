name: CI — Lint and Validate

on: [push, pull_request]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends libgdal-dev libgeos-dev libproj-dev libspatialindex-dev

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Syntax check
        run: python -m py_compile scripts/ml/07_xgboost_model.py scripts/ml/08_random_forest.py scripts/figures/generate_figures.py app.py

      - name: Run tests
        run: pytest tests/ -v --tb=short || echo "No tests found — skipping"

      - name: Dependency check
        run: pip check
